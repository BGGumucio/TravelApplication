var express = require('express');
var router = express.Router();
var searchCtrl = require('../controllers/searchCtrl');

// router.get('/',searchCtrl.stuff);
// router.get("/go", searchCtrl.test);

module.exports = router;
